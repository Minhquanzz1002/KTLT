/*Tinh do dai chuoi su dung con tro*/
#include <stdio.h>
#define MAX 100
#include <string.h>
int main(){
    char str[MAX];
    int len;
    printf("Enter string: ");
    fgets(str,sizeof(str),stdin);
    
}